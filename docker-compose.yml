version: '3.9'

services:
  db:
    image: postgres:latest
    restart: always
    environment:
      - POSTGRES_USER=fmadmin
      - POSTGRES_PASSWORD=fmpassword
      - POSTGRES_DB=fmdb
    ports:
      - '5432:5432'